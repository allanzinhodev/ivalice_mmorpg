<?xml version="1.0" encoding="UTF-8"?>
<outfits>
	<!-- Datapack minimo do ivalice: por ora APENAS o looktype 1.

	     Os looktypes 2-4 existiam e foram removidos de proposito. No .dat os
	     quatro apontavam para exatamente os mesmos sprites, entao trocar de
	     outfit funcionava mas nao mudava um pixel -- o que fazia a janela
	     parecer quebrada e atrapalhava isolar o teste de COR, que e o que
	     esta sendo feito agora. Voltam quando cada um tiver sprite proprio.

	     ATENCAO: o mapa de outfits e indexado SO pelo looktype
	     (outfit.cpp:62-64), e getOutfitByLookType(lookType, sex) exige
	     que o sexo bata (outfit.cpp:192-198). Ou seja, cada looktype
	     pertence a UM sexo apenas -- declarar o mesmo looktype para os
	     dois gera "Duplicate looktype ignored" e o segundo e descartado.

	     Por isso e type="0" (feminino), casando com o sex=0 dos
	     personagens semeados no schema.sql. -->
	<outfit type="0" looktype="1" name="Outfit 1" enabled="yes" premium="no" unlocked="yes" />
</outfits>
